# 任务计划：实现 Godot MVP v0.1

## 目标
依据冻结 GDD，持续实现可在 Android 横屏运行的第三人称平面 PvE 射击游戏；本轮先交付可启动、可操作、可自动验证的核心垂直切片。

## 当前阶段
阶段 5（本轮完成）

## 各阶段

### 阶段 1：需求与仓库发现
- [x] 确认冻结 GDD 与 76 份 ADR
- [x] 检查 Git、仓库内容和项目说明
- [x] 将发现记录到 findings.md
- **状态：** complete

### 阶段 2：工程骨架与架构
- [x] 创建 Godot 4 工程与目录结构
- [x] 建立碰撞层、输入动作和数据模型
- [x] 创建可运行主场景
- **状态：** complete

### 阶段 3：首条垂直切片
- [x] 实现固定斜俯视镜头
- [x] 实现玩家移动、朝向、生命与标准手枪
- [x] 实现模块墙、基础射线射击和瞄准线
- [x] 加入键鼠调试输入，为后续 Android 触控复用同一命令接口
- **状态：** complete

### 阶段 3B：Android 触控输入
- [x] 实现可复用虚拟摇杆控件
- [x] 实现可拖动调向的独立射击键
- [x] 将触控与玩家公开命令接口连接
- [x] 在主场景加入横屏触控布局
- **状态：** complete

### 阶段 4：测试与验证
- [x] 运行 Godot headless 导入/语法检查
- [x] 运行自动化基础测试
- [x] 修复发现的问题并记录结果
- **状态：** complete

### 阶段 5：交付
- [x] 检查新增文件和 Git 状态
- [x] 更新进度与下一阶段计划
- [x] 向用户交付可运行结果
- **状态：** complete

## 关键问题
1. ~~当前环境安装的 Godot 4 具体版本是什么？~~ Godot 4.7 stable。
2. ~~首条切片在不依赖外部美术资源时能否完整运行和验证？~~ 可以；语法检查和自动场景测试通过。

## 已做决策
| 决策 | 理由 |
|------|------|
| 以冻结的 GDD-MVP-v0.1.md 为唯一玩法基线 | 避免实现阶段重新发明规则 |
| 先做玩家、相机、射击和墙体的垂直切片 | 最快验证操控、尺度与射线架构 |
| 增加键鼠调试输入，触控调用同一玩家命令接口 | headless/桌面开发可测，同时不改变 Android 优先级 |
| 首轮使用程序生成的低多边形基础网格 | 不依赖外部素材即可运行和验证 |

## 遇到的错误
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| 规划前发现用户所述 `.gitignore` 实际不存在 | 1 | 在工程初始化阶段补建标准 Godot `.gitignore` |
| 首次 `--check-only` 无法识别跨脚本 `class_name` 类型 | 1 | 先运行 headless 编辑器导入生成 Godot 全局类缓存，再复查；若仍失败改用显式 preload |
| macOS headless 启动主场景时因 `user://logs` 不可写并在退出阶段崩溃 | 3 | 三种启动方式均复现，停止重复；依赖 check-only、场景测试与后续真机验证，项目保持 Mobile |
| 自定义 `VirtualJoystick` 与 Godot 4.7 新增原生类重名 | 1 | 触控类改用 `GameVirtualJoystick`、`GameFireAimButton`、`GameTouchInputRouter` 命名 |
| 沙箱内 `git add` 无法创建 `.git/index.lock` | 1 | 使用用户已明确授权的 Git 提权路径执行暂存与提交 |

## 备注
- 核心规则变化必须新增 ADR；普通数值可在资源文件中调优。
- 每阶段完成后同步更新 progress.md。
