# 进度日志

## 会话：2026-09-04

### 阶段 61：按体验多样性规范重新规划当前地图
- **状态：** complete
- 用户要求按照新建立的体验多样性规范重新规划当前地图。
- 已按 `planning-with-files-zh` 恢复 `task_plan.md`、`findings.md`、`progress.md` 和工作区差异。
- 确认本轮交付边界为地图设计规划与实施门禁，不直接修改 Godot 场景。
- 下一步逐项审计 `cqb_room_layout_3d.tscn`、主场景固定环境物和 `ContentSpawner3D` 候选点，建立现状空间模型。
- 审计确认宏观 CQB 骨架、错位门洞、出生安全和左右循环可保留；体验重复主要来自东西镜像微观布局与警卫/武器/医疗独立洗牌。
- 使用现有视觉探针重新生成并检查 `/tmp/gogogo-cqb-room-layout.png`，确认北排房间重复“单掩体＋水平木墙”，西南连锁突出而东侧缺少不同性质的问题。
- 新建 `docs/design/CURRENT-MAP-VARIETY-REPLAN.md`，将 12 个房间、两侧外环和出生庭院组织为 15 个规划区域。
- 冻结三条路线主题：西线高波动环境捷径、中线信息与目标排序、东线稳定补给路线。
- 完成逐区域遭遇矩阵，覆盖 7 类模板，并为每个非安全区域记录主要决策、两个解法族、成本、奖励和任务激活后变化。
- 规划内容生成由散点洗牌升级为先选任务侧与遭遇组合，再分配警卫、适配武器和风险医疗；保留原数量预算和种子语义。
- 形成 A–E 五个实施切片：地图语义化、打破镜像、遭遇组合生成器、结构回归、五种子可玩审计。
- 设计层验收通过；场景实现和五种子验证尚未执行，因此文档明确保持“规划完成，场景实现与验证待执行”。

## 会话：2026-08-31

### 阶段 60：实际声音表现接入
- **状态：** complete
- 执行的操作：
  - 检查 `assets/audio/prototypes` 中 5 个声音资源及项目引用，确认尚未接入实际播放器。
  - 审计声音事件中心、武器定义、玩家脚步和环境爆炸链路，确认现有逻辑事件已覆盖实际播放所需的位置与时序。
  - 决定在 `GameSoundEventHub` 内实现可重叠的 3D 音频播放池，并按事件类型/武器定义选择资源。
  - 使用 ffprobe 检查全部音频时长、编码和声道，确认机枪与脚步是长录音，实际播放必须按事件截取而不能整段叠加。
  - 实现 16 路 `AudioStreamPlayer3D` 播放池、优先级默认 cue、武器专属 cue、脚步交替取样和长录音截断。
  - 首次编辑器导入成功识别全部 5 个音频；脚本检查发现播放池复用值被推断为 Variant，已增加显式类型标注。
  - 移除 `GameShotFeedback3D` 的程序化占位枪声，避免与实际录音叠加；视觉、后坐和移动端震动反馈不变。
  - 新增音频资源映射、播放池类型、脚步交替和重叠播放回归，并同步 README。
  - 常规语法与完整核心回归通过；为定位退出期既有资源警告追加的 `--verbose` 运行命中已知 `user://logs` / MoltenVK signal 11，按既有策略不重复该环境路径。
  - 独立音频中心探针创建、释放并干净退出，确认完整套件的既有 ObjectDB 警告不是新播放池泄漏。
  - 最终 Godot `--check-only`、完整核心回归和 `git diff --check` 均通过，核心回归输出 `PASS: core slice tests`。
  - 新增原始音频文件名映射与发行前许可证核验提醒，保留作者和 Freesound 编号线索。

## 会话：2026-08-25

### 阶段 1：需求与仓库发现
- **状态：** complete
- 执行的操作：
  - 读取 planning-with-files-zh 完整规则与模板。
  - 检查 Git、仓库内容、项目说明与 `.gitignore`。
  - 确认冻结 GDD 是实现基线。
- 创建/修改的文件：
  - `.gitignore`
  - `task_plan.md`
  - `findings.md`
  - `progress.md`

### 阶段 2：工程骨架与架构
- **状态：** complete
- 执行的操作：
  - 创建 Godot 4.7 Mobile 工程配置与项目目录。
  - 建立 WeaponDefinition、HealthComponent、HitscanWeapon 等基础领域组件。
  - 创建玩家、可损坏木墙与主场景。
- 创建/修改的文件：
  - `project.godot`
  - `resources/weapons/standard_pistol.tres`
  - `scripts/resources/weapon_definition.gd`
  - `scripts/components/health_component.gd`
  - `scripts/weapons/hitscan_weapon.gd`
  - `scenes/actors/player.tscn`
  - `scenes/world/damageable_wall.tscn`
  - `scenes/main/main.tscn`

### 阶段 3：首条垂直切片
- **状态：** complete
- 执行的操作：
  - 实现固定方位高位跟随镜头。
  - 实现 4.5 m/s 独立移动、鼠标水平面瞄准和保持朝向。
  - 实现标准手枪射线、6 发弹匣、自动换弹与 16 米射程。
  - 实现常驻分段瞄准线和 5 点耐久木墙。
  - 添加 HUD 与键鼠调试说明。
- 创建/修改的文件：
  - `scripts/actors/player.gd`
  - `scripts/input/debug_player_input.gd`
  - `scripts/camera/fixed_follow_camera.gd`
  - `scripts/weapons/aim_line_3d.gd`
  - `scripts/world/damageable_wall.gd`
  - `scripts/main/main.gd`

### 阶段 4：测试与验证
- **状态：** complete
- 执行的操作：
  - 创建无第三方测试框架的 headless 测试入口。
  - 完成 Godot 全局类导入与语法检查。
  - 验证武器资源、生命组件、场景结构、触控节点和射线伤害链路。
- 创建/修改的文件：
  - `tests/test_runner.gd`

### 阶段 5：交付
- **状态：** complete
- 执行的操作：
  - 添加根目录 README、运行与验证说明。
  - 确认本轮切片完成，下一阶段为视野与警卫 AI。
- 创建/修改的文件：
  - `README.md`

### 阶段 6：对称视野与遮挡
- **状态：** complete
- 执行的操作：
  - 提交首条 Godot 核心切片：`b06c6ca`。
  - 复查玩家、武器、主场景与测试结构。
  - 确定共享 VisionSensor3D 与射线裁切视野网格方案。
  - 实现玩家和警卫共用的 120° / 16 米视觉传感器。
  - 实现受砖墙和木墙射线裁切的地面扇形网格。
- 创建/修改的文件：
  - `task_plan.md`
  - `findings.md`
  - `progress.md`
  - `scripts/perception/vision_sensor_3d.gd`
  - `scripts/perception/vision_cone_3d.gd`
  - `scenes/actors/player.tscn`

### 阶段 7：手枪警卫感知闭环
- **状态：** complete
- 执行的操作：
  - 实现 3 点生命、标准手枪和对称视野警卫。
  - 实现巡逻、预警、战斗、搜索状态与首枪时序。
  - 实现 2 秒攻击暴露、墙体遮挡和最后暴露位置标记。
- 创建/修改的文件：
  - `scripts/actors/pistol_guard.gd`
  - `scenes/actors/pistol_guard.tscn`
  - `scenes/main/main.tscn`
  - `scripts/main/main.gd`

### 阶段 8：感知与战斗验证
- **状态：** complete
- 执行的操作：
  - 自动验证视野角度、距离、墙体遮挡和双方对称性。
  - 自动验证 0.6 秒首枪造成伤害与 2 秒攻击暴露。
  - 自动验证背后显形、墙后隐藏与静止位置标记。
- 创建/修改的文件：
  - `tests/test_runner.gd`

### 阶段 9：第二切片交付
- **状态：** complete
- 执行的操作：
  - 更新 README 和计划记录。
  - 完成语法、回归与 Git 差异检查。
- 创建/修改的文件：
  - `README.md`

### 阶段 10：声音事件领域层
- **状态：** complete
- 执行的操作：
  - 提交感知与警卫切片：`8a0a939`。
  - 确定 SoundEventHub、武器声音数据和警卫调查状态方案。
  - 实现集中式声音事件、武器枪声、玩家脚步声与木墙摧毁声。
- 创建/修改的文件：
  - `task_plan.md`
  - `findings.md`
  - `progress.md`
  - `scripts/audio/sound_event_hub.gd`
  - `scripts/resources/weapon_definition.gd`
  - `resources/weapons/standard_pistol.tres`
  - `scripts/weapons/hitscan_weapon.gd`
  - `scripts/actors/player.gd`
  - `scripts/world/damageable_wall.gd`

### 阶段 11：警卫听声调查
- **状态：** complete
- 执行的操作：
  - 为警卫增加 INVESTIGATE 状态、声源优先级替换和目视覆盖规则。
  - 实现前往声源、现场扫描、超时返回巡逻的闭环。
- 创建/修改的文件：
  - `scripts/actors/pistol_guard.gd`
  - `scenes/main/main.tscn`

### 阶段 12：声音切片验证
- **状态：** complete
- 执行的操作：
  - 自动验证标准手枪 24 米声音半径。
  - 自动验证警卫在半径内调查、半径外保持巡逻。
  - 完成语法检查与完整回归测试。
- 创建/修改的文件：
  - `tests/test_runner.gd`
  - `README.md`

### 阶段 13：二维格子导航层
- **状态：** complete
- 执行的操作：
  - 提交声音事件与警卫调查切片：`9806709`。
  - 复查 ADR-063、064、065、066、069，确定 2 米正交格与导航版本重算方案。
  - 实现 AStarGrid2D 世界服务、碰撞体占格、世界坐标转换和路径查询。
- 创建/修改的文件：
  - `task_plan.md`
  - `progress.md`
  - `scripts/navigation/grid_navigation_3d.gd`
  - `scenes/main/main.tscn`

### 阶段 14：动态开墙与警卫重算
- **状态：** complete
- 执行的操作：
  - 木墙进入场景时注册阻塞格，摧毁时同步注销并递增导航版本。
  - 警卫调查和搜索移动改为沿正交格点路径推进。
  - 警卫检测目标或导航版本变化并重新规划。
- 创建/修改的文件：
  - `scripts/world/damageable_wall.gd`
  - `scripts/actors/pistol_guard.gd`

### 阶段 15：导航验证
- **状态：** complete
- 执行的操作：
  - 验证完整木墙阻塞、路径绕行、摧毁后即时开格和路线缩短。
  - 验证导航版本变化后警卫使用新版本重算。
  - 完成语法检查与完整回归测试。
- 创建/修改的文件：
  - `tests/test_runner.gd`
  - `README.md`

### 阶段 16：安全掩体候选
- **状态：** complete
- 执行的操作：
  - 复查掩体、动态破坏和二维导航规则。
  - 确定以阻塞格四邻域生成候选，并以物理射线、路径可达性和路径长度筛选。
  - 实现 6 米内阻塞格候选、长墙两步探身点和最近路径选择。
- 创建/修改的文件：
  - `task_plan.md`
  - `findings.md`
  - `progress.md`
  - `scripts/ai/cover_service_3d.gd`
  - `scripts/navigation/grid_navigation_3d.gd`
  - `scenes/main/main.tscn`

### 阶段 17：警卫掩体战斗
- **状态：** complete
- 执行的操作：
  - 增加 MOVE_TO_COVER 与 IN_COVER 状态。
  - 实现沿格子路径进入掩体、隐藏计时、移动探身、0.2 秒锁向单发和回撤。
  - 导航或遮挡变化时立即复核掩体；失效后重选或回到移动战斗。
  - 无掩体时实现小于 8 米后退、大于 12 米接近、区间内横向移动。
- 创建/修改的文件：
  - `scripts/actors/pistol_guard.gd`

### 阶段 18：掩体切片验证
- **状态：** complete
- 执行的操作：
  - 验证安全候选、可达性、探身清晰射线和锁向单发。
  - 验证木墙摧毁后候选同逻辑帧失效且警卫放弃无替代掩体。
  - 验证近距后退、远距接近和 8–12 米横移。
  - 完成语法检查与完整回归测试。
- 创建/修改的文件：
  - `tests/test_runner.gd`
  - `README.md`

### 阶段 19：环境反应领域层
- **状态：** complete
- 执行的操作：
  - 提交警卫掩体战斗切片：`e85b8f7`。
  - 复查 ADR-020 至 024、049 至 053、061，冻结首轮燃烧、爆炸、声音和连锁数值。
  - 确定爆炸 FIFO 队列、预伤害遮挡快照和 2 米严格火焰邻接方案。
  - 实现无阵营爆炸目标快照、线性衰减、墙体遮挡和 40 米爆炸声音。
  - 实现严格邻接火焰队列、每秒 1 点角色伤害和 8 米燃烧声音。
- 创建/修改的文件：
  - `task_plan.md`
  - `findings.md`
  - `progress.md`
  - `scripts/world/environment_reaction_hub.gd`
  - `scripts/actors/player.gd`
  - `scripts/actors/pistol_guard.gd`

### 阶段 20：石油与汽油滚筒模块
- **状态：** complete
- 执行的操作：
  - 创建共享环境模块基类，统一导航、碰撞、视觉和活动状态生命周期。
  - 木墙增加 3 秒燃烧、火区伤害和邻接传播。
  - 石油滚筒实现 4 耐久、4 秒燃烧与燃尽开格。
  - 汽油滚筒实现 3 耐久、接火即爆、3 米衰减爆炸与即时开格。
  - 在主场景加入石油→木墙→汽油的可观察连锁排列。
- 创建/修改的文件：
  - `scripts/world/environment_module_3d.gd`
  - `scripts/world/damageable_wall.gd`
  - `scripts/world/oil_barrel_wall.gd`
  - `scripts/world/gasoline_barrel_wall.gd`
  - `scenes/world/damageable_wall.tscn`
  - `scenes/world/oil_barrel_wall.tscn`
  - `scenes/world/gasoline_barrel_wall.tscn`
  - `scenes/main/main.tscn`

### 阶段 21：环境连锁验证
- **状态：** complete
- 执行的操作：
  - 验证石油点燃木墙、木墙触发汽油爆炸的确定性链。
  - 验证斜角不传播、火区每秒 1 点伤害、3/4 秒燃尽和即时开格关碰撞。
  - 验证中心/边缘爆炸伤害、玩家自伤、警卫受伤、砖墙遮挡和同次不穿透。
  - 完成语法检查与完整回归测试。
- 创建/修改的文件：
  - `tests/test_runner.gd`
  - `README.md`

### 阶段 22：多武器领域层
- **状态：** complete
- 执行的操作：
  - 提交环境燃烧与爆炸切片：`bce524d`。
  - 复查 ADR-013、016 至 019、045、048、054、072、073，冻结双槽、数值、拾取与触控规则。
  - 确定共享武器基类、射线/火箭派生、运行时特殊槽和上下文拾取方案。
  - 提取 `GameWeapon3D`，统一弹匣、有限/无限备用弹药、冷却、自动换弹、声音与弹药耗尽信号。
  - 扩展武器定义，加入射线/火箭类型、机枪散布和火箭飞行/爆炸参数。
  - 新增重型手枪、机枪和火箭枪资源，并将射线武器迁移到共享基类。
  - 实现实体火箭、玩家永久手枪加特殊槽、动态瞄准线与键鼠切枪/自动武器输入。
  - 实现地图拾取的空槽自动拾取、同类补弹、异类确认交换及旧武器带弹药掉落。
  - 在主场景放置四件特殊武器拾取物，并加入默认/特殊切换和临时交换按钮。
- 创建/修改的文件：
  - `task_plan.md`
  - `findings.md`
  - `progress.md`
  - `scripts/resources/weapon_definition.gd`
  - `scripts/weapons/weapon_3d.gd`
  - `scripts/weapons/hitscan_weapon.gd`
  - `scripts/weapons/rocket_weapon.gd`
  - `scripts/weapons/rocket_projectile.gd`
  - `scripts/weapons/aim_line_3d.gd`
  - `scripts/actors/player.gd`
  - `scripts/input/debug_player_input.gd`
  - `scripts/world/weapon_pickup_3d.gd`
  - `scripts/main/main.gd`
  - `resources/weapons/standard_pistol.tres`
  - `resources/weapons/heavy_pistol.tres`
  - `resources/weapons/machine_gun.tres`
  - `resources/weapons/rocket_launcher.tres`
  - `scenes/weapons/rocket_projectile.tscn`
  - `scenes/world/weapon_pickup_3d.tscn`
  - `scenes/main/main.tscn`

### 阶段 23：实体火箭与双武器槽
- **状态：** complete
- 执行的操作：
  - 玩家永久保留标准手枪，并以运行时武器节点承载一个特殊槽。
  - 实现 10 m/s、16 米航程、层 1/2/8 碰撞检测的实体火箭和 4 米无阵营爆炸。
  - 瞄准线、HUD、键鼠及触控射击语义跟随当前武器；特殊弹药耗尽自动回手枪。

### 阶段 24：地图武器拾取
- **状态：** complete
- 执行的操作：
  - 实现空槽自动装备、同类补充备用弹药、满弹不消耗拾取物。
  - 实现异类临时确认按钮，换下武器在原地保留弹匣和备用弹药掉落。
  - 主场景放置重型手枪、机枪、火箭枪和额外机枪四件特殊武器。

### 阶段 25：多武器验证与提交
- **状态：** complete
- 执行的操作：
  - 自动验证三类特殊武器冻结数值、有限备用弹药、自动换弹和机枪散布恢复。
  - 自动验证实体火箭生成、统一射程、碰撞警卫和环境爆炸伤害。
  - 自动验证拾取、补弹、确认交换、掉落状态保留及耗尽回手枪。
  - Godot 语法检查与完整核心回归测试通过；README 已同步。
  - `git diff --check` 通过，工作区差异仅包含本切片相关代码、场景、资源、测试和规划文档。

### 阶段 26：任务与撤离领域层
- **状态：** complete
- 执行的操作：
  - 提交多武器、地图拾取与实体火箭切片：`a5d58eb`。
  - 复查 GDD 单局循环、HUD、验收规则及 ADR-028 至 033、056、071。
  - 冻结本切片边界：3 秒范围激活、离开清零、受伤不中断、任务点警报、出生点即时撤离、方向/距离/敌人数 HUD 与胜负结算。
  - 创建任务点 Area3D，提供自动激活、离开清零、进度与完成信号。
  - 创建撤离 Area3D，任务完成前禁用、启用后玩家进入立即发出撤离事件。
  - 创建任务控制器，统一两阶段状态、用时、击杀、剩余警卫、胜利与死亡失败。

### 阶段 27：全体警戒与任务 HUD
- **状态：** complete
- 执行的操作：
  - 为警卫增加公开任务警报入口，强制调查任务点位置但不持续获得玩家坐标。
  - 创建屏幕边缘目标箭头，以近/中/远显示大致距离，并在激活后切换到出生撤离点。
  - HUD 增加剩余警卫、任务阶段、3 秒激活条和用时/击杀/生命/撤离结算。

### 阶段 28：主场景胜利闭环
- **状态：** complete
- 执行的操作：
  - 在 `(28, 0, -19)` 放置远端任务点，在玩家出生位置 `(0, 0, 5)` 放置撤离区。
  - 使用发光 PrimitiveMesh 和 Label3D 表现未激活任务、已激活任务、禁用撤离与启用撤离。
  - 主场景接入任务控制器，实现去程、激活、全体警戒、回程、即时撤离和死亡失败。

### 阶段 29：任务闭环验证与提交
- **状态：** complete
- 执行的操作：
  - 自动验证任务点进度、离开清零、受伤不中断和三秒激活。
  - 自动验证警卫只获得任务点位置，玩家移动后不会被警报持续追踪。
  - 自动验证激活前无法撤离、任务激活后仍有警卫也能即时获胜、死亡立即失败。
  - 自动验证目标距离分级、剩余警卫更新、胜负结算和结算后输入关闭。
  - Godot 语法检查与完整核心回归测试通过；README 已同步。
  - `git diff --check` 通过，差异仅包含任务/撤离闭环相关脚本、场景、HUD、测试和规划文档。

## 本轮错误日志
| 时间戳 | 错误 | 尝试次数 | 解决方案 |
|--------|------|---------|---------|
| 2026-08-26 | 任务点直线距离约 35.5 米，且撤离启用时读取旧重叠缓存导致测试传送后误判即时胜利 | 1 | 任务点移到 `(28, 0, -19)`；撤离只响应启用后的 `body_entered`，不在启用瞬间扫描可能陈旧的重叠集合 |

### 阶段 30：稳定随机种子生命周期
- **状态：** complete
- 执行的操作：
  - 提交任务激活、全体警戒、撤离与结算切片：`fc8bcd8`。
  - 复查 ADR-003、025、027、034、035、054、055、067 与 GDD 随机/生命/武器规则。
  - 确定 autoload 只保存本局种子，重试通过场景重载恢复全部初始状态；生成器使用独立 RNG，不污染全局随机序列。
  - 确定共享候选点、固定内容预算和生成器先于任务控制器运行的场景树方案。
  - 创建 `GameRunState` autoload，新游戏生成种子，重试复用当前种子。
  - 结算面板增加“重试本局”和“新游戏”，分别保留或替换种子后重载场景。

### 阶段 31：手工候选点与内容生成器
- **状态：** complete
- 执行的操作：
  - 主场景加入 16 个警卫、4 个远端任务、10 个武器与 8 个急救包候选点。
  - 创建独立 RNG 内容生成器，一次性抽取 8–12 名警卫、1 个任务点、4 件武器与 2 个急救包。
  - 武器预算保证重型手枪、机枪、火箭枪各一件，第四件均匀随机重复。
  - 生成器记录完整布局签名，用于稳定重试的精确回归验证。

### 阶段 32：急救包闭环
- **状态：** complete
- 执行的操作：
  - 创建自动拾取急救包，受伤时恢复 2 格，满血不消耗且恢复不超过上限。
  - 急救包和武器拾取物表现层跟随玩家 120° 视野与墙体遮挡隐藏。

### 阶段 33：生成系统接入
- **状态：** complete
- 执行的操作：
  - 固定演示警卫、任务点和武器替换为运行时生成节点，同时保留现有名称供任务、HUD 与测试绑定。
  - 任务控制器在生成后扫描所有预置警卫，HUD 自动显示随机剩余数量。
  - 既有测试统一冻结当前场景 `guards` 分组，避免其余随机警卫后台 AI 污染断言。

### 阶段 34：随机布局验证与提交
- **状态：** complete
- 执行的操作：
  - 自动验证固定种子布局签名完全相同，另一个种子产生不同布局。
  - 自动验证所有生成内容来自对应候选点，警卫数量 8–12 且不在出生点初始直接视野内。
  - 自动验证三类保证武器加随机重复、两个急救包和远端任务距离预算。
  - 自动验证满血不消耗、受伤恢复 2 格、动态物品盲区隐藏，以及重试恢复生命与已消耗急救包。
  - Godot 语法检查与完整核心回归测试通过；README 已同步。
  - `git diff --check` 通过，差异仅包含稳定种子、候选点生成、急救包、结算入口、测试与规划文档。

## 本轮随机生成错误日志
| 时间戳 | 错误 | 尝试次数 | 解决方案 |
|--------|------|---------|---------|
| 2026-08-26 | 生成器 `_ready` 向仍在装配子节点的主场景父节点添加兄弟失败，任务控制器随后找不到任务点 | 1 | 生成器改为 `Node3D` 内容容器，随机实例同步添加为其子节点；任务与 HUD 路径改为容器内固定名称，避免 deferred 竞态 |
| 2026-08-26 | `--script` 测试入口在 autoload 全局标识符注册前编译，直接引用 `GameRunState` 失败 | 1 | 测试从 SceneTree `/root/GameRunState` 动态获取真实 autoload；生产场景仍使用已注册单例 API |
| 2026-08-26 | `--script` 递归编译场景依赖时，生成器中的 autoload 标识符同样早于注册 | 2 | 生成器和结算按钮统一从 `/root/GameRunState` 调用单例 API，彻底移除编译期 autoload 名称依赖 |

## 测试结果
| 测试 | 输入 | 预期结果 | 实际结果 | 状态 |
|------|------|---------|---------|------|
| 仓库检查 | Git 状态与文件列表 | 确认初始状态 | Git 已初始化，仅 docs 未跟踪 | 通过 |
| Godot 版本 | `godot --version` | Godot 4 可用 | 4.7 stable | 通过 |
| GDScript 语法 | `--check-only --quit` | 无解析错误 | 无错误输出 | 通过 |
| 核心场景测试 | `tests/test_runner.gd` | 资源、场景和组件可运行 | `PASS: core slice tests` | 通过 |
| 射线集成 | 玩家向木墙射击一次 | 弹药 6→5、耐久 5→4 | 符合预期 | 通过 |
| 对称视野 | 玩家与警卫互相观察 | 同为 120°、16 米、受墙遮挡 | 符合预期 | 通过 |
| 警卫首枪 | 等待 0.6 秒预警 | 玩家受 1 点伤害 | 符合预期 | 通过 |
| 攻击暴露 | 背后开火、随后进入墙后 | 显形、墙后隐藏、静止标记 | 符合预期 | 通过 |
| 声音调查 | 半径内、半径外各发出 24 米枪声 | 近处警卫调查、远处警卫保持巡逻 | 符合预期 | 通过 |
| 动态格子导航 | 查询完整墙路线后摧毁单个模块 | 初始绕行、开格后路线缩短、警卫重算 | 符合预期 | 通过 |
| 警卫掩体战斗 | 选择长墙掩体、探身射击、摧毁墙体 | 最近安全候选、单发后回撤、失效后放弃或重选 | 符合预期 | 通过 |
| 无掩体移动 | 分别设置 5 米、15 米、10 米距离 | 后退、接近、横向移动 | 符合预期 | 通过 |
| 环境火焰 | 石油点燃、正交/斜角模块与角色入火区 | 正交连锁、斜角不传播、每秒 1 点、燃尽开格 | 符合预期 | 通过 |
| 汽油爆炸 | 中心玩家、边缘警卫、砖墙后玩家 | 3/1 点衰减、无阵营自伤、墙后免伤 | 符合预期 | 通过 |
| 爆炸遮挡快照 | 爆炸同时摧毁弱化木墙 | 墙后玩家不受同次爆炸伤害 | 符合预期 | 通过 |
| 多武器领域 | 三类特殊武器资源、有限弹药、换弹和机枪持续请求 | 数值有效、备用弹药正确消耗、按住射击连续受射速约束 | 符合预期 | 通过 |
| 武器拾取交换 | 空槽、同类、异类和满备用弹药场景 | 自动装备/补弹、确认交换、旧武器保留状态、满弹不消耗 | 符合预期 | 通过 |
| 实体火箭 | 10 m/s 飞行并对齐警卫碰撞轨迹 | 16 米上限、4 米爆炸、命中层 8 警卫并造成中心伤害 | 符合预期 | 通过 |
| 任务激活 | 进入、受伤、离开、再次停留 3 秒 | 受伤不中断、离开清零、完成后进入撤离阶段 | 符合预期 | 通过 |
| 全体警戒 | 激活任务后移动玩家 | 警卫调查任务点，警报不持续提供玩家坐标 | 符合预期 | 通过 |
| 撤离胜负 | 激活前进入出生点、激活后带 1 名存活警卫返回、另局死亡 | 提前不可撤离、无需全灭即可即时获胜、死亡立即失败 | 符合预期 | 通过 |
| 稳定随机布局 | 固定种子两次加载与不同种子加载 | 同种子四类内容签名完全一致，不同种子布局不同 | 符合预期 | 通过 |
| 内容预算 | 检查警卫、任务、武器和急救包候选抽取 | 8–12 警卫、远端任务、三类必出加一重复、两个急救包 | 符合预期 | 通过 |
| 急救与重试 | 满血/受伤拾取后以同种子重载 | 满血不消耗、恢复 2 格、重试恢复生命和物资 | 符合预期 | 通过 |

## 错误日志
| 时间戳 | 错误 | 尝试次数 | 解决方案 |
|--------|------|---------|---------|
| 2026-08-25 | 用户所述 `.gitignore` 实际不存在 | 1 | 创建标准 Godot `.gitignore` |
| 2026-08-25 | 首次 `--check-only` 无法解析跨脚本 `class_name` 类型 | 1 | 改为先用 headless 编辑器导入生成类缓存，再复查 |
| 2026-08-25 | macOS headless 启动主场景时 `user://logs` 不可写并 signal 11 | 3 | 三种方式均复现，停止重复；改以 check-only、场景测试和后续真机验证覆盖 |
| 2026-08-25 | 自定义 VirtualJoystick 隐藏 Godot 4.7 原生类 | 1 | 三个触控类统一增加 Game 前缀，避免引擎命名冲突 |
| 2026-08-25 | 沙箱内 git add 无法写 `.git/index.lock` | 1 | 改用明确授权的 escalated Git 暂存与提交 |
| 2026-08-25 | ImmediateMesh 视野网格触发 macOS headless 渲染崩溃 | 1 | headless 跳过纯视觉网格，保留感知和 AI 自动测试 |
| 2026-08-25 | 连续 Godot 验证进程再次触发沙箱/MoltenVK 崩溃 | 1 | 拆分为独立测试进程并显式使用兼容后端 |
| 2026-08-25 | 新增 GameGridNavigation3D 后首次 check-only 未识别全局类 | 1 | 先独立执行 Godot 编辑器导入以刷新脚本类缓存，再复查 |
| 2026-08-25 | Godot 无法推断四邻域数组字面量的循环变量类型 | 1 | 使用显式 `Array[Vector2i]` 常量和变量类型标注 |
| 2026-08-25 | 掩体测试在连续长墙上找不到一格探身点 | 1 | 扩展为掩体格周围两步内的最近可达清晰点，保留全部安全筛选 |
| 2026-08-25 | 已摧毁木墙的碰撞在帧末前仍使掩体射线命中 | 1 | 射线忽略导航已注销的碰撞体并继续检测，实现同逻辑帧失效 |
| 2026-08-25 | 后退测试沿用探身结束位置而落入横移距离带 | 1 | 断言前复位警卫到 5 米距离，单独验证近距后退分支 |
| 2026-08-25 | 火焰队列 pop_front 的 Variant 推断被严格类型检查拦截 | 1 | 显式转换为 Node3D，保留类型安全 |
| 2026-08-25 | 环境连锁新增木墙后旧测试数量断言失败 | 1 | 更新为 5 段并补充两类滚筒及环境 Hub 场景断言 |
| 2026-08-26 | 扩展武器回归中火箭未在预期时限伤到警卫，既有横移断言同时偶发失败 | 1 | 火箭碰撞掩码补入警卫层 8；横移测试改用移动前径向向量，避免 `move_and_slide` 位移后测量引入伪径向分量 |
| 2026-08-26 | 火箭补入警卫层后测试仍未命中 | 2 | 发现枪口横向偏移 0.45 米而警卫胶囊半径 0.4 米；测试目标改为对齐真实枪口/虚线轴线，保留实际弹道语义 |
| 2026-08-26 | 火箭命中击杀后测试继续读取已释放警卫并提前结束，造成伪 PASS | 3 | 改用 `HealthComponent.depleted` 信号记录命中结果，并增加集成测试完成标记，运行时异常不能再伪装为全通过 |
| 2026-08-26 | 新增按住连射断言后，紧接的最终弹药场景仍处于上一发冷却 | 1 | 独立场景断言前显式推进一个射击间隔，避免跨测试状态污染；武器正确保留冷却约束 |

## 五问重启检查
| 问题 | 答案 |
|------|------|
| 我在哪里？ | 阶段 44：Android CI SDK 修复已完成本地验证，等待远端 Action 复验 |
| 我要去哪里？ | 推送配置后手动触发 Android Build，确认 APK 已发布到对应 GitHub Release |
| 目标是什么？ | 开始实现冻结 MVP，并交付可运行、可测试的核心切片 |
| 我学到了什么？ | 见 findings.md |
| 我做了什么？ | 已完成任务撤离、多武器、随机内容、稳定重试、感知、瞄准吸附、盲区表现、Android CI 构建、导航、掩体、燃烧与爆炸闭环 |

---
*每个阶段完成后或遇到错误时更新此文件*

### 阶段 35：轻度触屏瞄准吸附
- **状态：** complete
- 执行的操作：
  - 恢复文件规划上下文，确认工作区干净且上一切片已提交为 `c86bdff`。
  - 复查 ADR-007、ADR-009、ADR-075 及 GDD 视野/触控要求。
  - 确定本切片分为轻度瞄准吸附、静态盲区暗化、动态隐藏和盲区环境间接反馈四个可测边界。
  - 创建 `GameAimAssist3D`，首轮参数为 10° 候选锥、40% 插值且单帧最多修正 3°；只选择玩家有效视野内的存活警卫。
  - 玩家朝向、虚线和实际开火共享同一个修正后 `aim_direction`，吸附查询不改变弹药或触发开火。

### 阶段 36：完整盲区表现
- **状态：** complete
- 执行的操作：
  - 创建 `GameWorldVisibility3D`，按静态墙段/模块中心与遮挡边界切换原色和暗化降饱和材质。
  - 地板、砖墙与所有环境模块纳入静态可见性组；已有警卫/拾取物隐藏规则保持不变。

### 阶段 37：盲区环境事件间接反馈
- **状态：** complete
- 执行的操作：
  - 场景可见性服务监听火焰传播和爆炸结算，盲区火焰隐藏 `FireIndicator`。
  - HUD 增加 1 秒的火焰/爆炸八方向箭头，只传递事件类型和方向。

## 本轮可见性错误日志
| 时间戳 | 错误 | 尝试次数 | 解决方案 |
|--------|------|---------|---------|
| 2026-08-26 | 沙箱内 `--check-only` 无法写入 `user://logs` 并在 MoltenVK 路径 signal 11 | 1 | 停止沙箱内重试，改用已授权的非沙箱 headless 命令 |
| 2026-08-26 | 瞄准吸附正例测试把候选警卫放在了木墙后，组件正确拒绝选择 | 1 | 将正例目标移到墙前 5 米内，墙后拒绝保留为独立反例 |

### 阶段 38：可见性切片验证与提交
- **状态：** complete
- 执行的操作：
  - 自动验证多目标优选、有限修正、不自动开火，以及墙后、背后、超距三类拒绝条件。
  - 自动验证盲区静态墙体暗化但不隐藏、动态警卫隐藏、火焰精确效果隐藏，以及爆炸/火焰只产生短时方向反馈。
  - Godot 类缓存导入成功，非沙箱 `--check-only` 通过，完整核心回归输出 `PASS: core slice tests`。
  - `git diff --check` 通过，README 已同步本切片能力和下一阶段。

### 阶段 39：Android 导出预设
- **状态：** complete
- 执行的操作：
  - 恢复文件规划上下文，确认上一切片已提交为 `df75057` 且工作区干净。
  - 复查 ADR-057、ADR-059、ADR-060 与 GDD 技术验收边界。
  - 用户调整范围：不搭建本地 Android SDK，改为由 GitHub Actions 统一产出可安装调试 APK。
  - 创建 Android 导出预设：API 24+、arm64-v8a、调试签名、沉浸式边到边显示，复用工程横屏与 Mobile 渲染设置。
  - 使用 `--export-pack Android` 成功导出完整 PCK，证明 Godot 能解析预设并编译所有资源/脚本。

### 阶段 40：GitHub Actions Android 构建
- **状态：** complete
- 执行的操作：
  - 新增 `Android Build` workflow，支持 `main` 推送、Pull Request 和手动触发。
  - 使用 `barichello/godot-ci:4.7` 容器复制导出模板与引擎设置，之后导入、检查、回归、导出。
  - 导出 `build/android/gogogo-debug.apk`，检查文件非空后使用 `actions/upload-artifact@v4` 保留 14 天。

### 阶段 41：CI 配置验证与提交
- **状态：** complete
- 执行的操作：
  - Ruby YAML 解析通过，Git ignore 正确排除本地 `build/`，关键导出/workflow 字段完整。
  - 核心测试增加 Android 预设、Godot CI 版本、回归前置和 artifact 上传断言。
  - Godot `--check-only` 通过，完整核心回归输出 `PASS: core slice tests`，`git diff --check` 通过。
  - README 已记录手动触发与 artifact 下载流程。

### 阶段 42：Android Build 手动触发收窄
- **状态：** complete
- 执行的操作：
  - 删除 workflow 的 `push` 和 `pull_request` 触发器，仅保留 `workflow_dispatch`。
  - README 同步改为仅通过 Actions 页面手动生成 APK。

### 阶段 43：Android CI SDK 环境诊断与修复
- **状态：** complete
- 执行的操作：
  - 从用户提供的原始 Action 日志确认致命症状为 Godot EditorSettings 缺少有效 Java SDK 路径，同时存在 fontconfig 缺失噪声。
  - 本机 Docker CLI 存在但 daemon 未运行，无法在同镜像中重放真实 APK 导出；原始 GitHub Action 日志作为原始红灯。
  - 官方 Dockerfile 确认 4.7 镜像的文件是 `editor_settings-4.7.tres`，旧 workflow 错误查找 `editor_settings-4.tres`。
  - 新增的5个环境回归断言在旧 workflow 上稳定失败，修复后全部转绿。
  - workflow 显式设置 `JAVA_HOME`、`ANDROID_HOME`、`ANDROID_SDK_ROOT`，复制正确版本 EditorSettings，并在导出前检查 Java、API 33、Build Tools 33.0.2 和 SDK 字段。
  - workflow 安装 `fontconfig`；Android Target SDK 与镜像已安装的 API 33 对齐，消除字体与 Build Tools 回退噪声。
  - YAML 及所有 Bash block 语法通过，Godot `--check-only`、完整核心回归和 Android PCK 导出全部通过。

### 阶段 44：GitHub Action 真实 APK 复验
- **状态：** pending
- 等待操作：
  - 推送本轮修复提交。
  - 在 GitHub Actions 页面手动触发 `Android Build`，回传结果以完成真实容器复验。

### 阶段 45：GitHub Release 发布
- **状态：** complete
- 执行的操作：
  - 手动触发参数增加版本标签与预发布开关，标签限制为语义化版本形式。
  - 标签版本写入 Android `version/name`，GitHub run number 写入递增的 `version/code`。
  - workflow 获得最小 `contents: write` 权限，并以 `softprops/action-gh-release@v2` 创建 Release、生成发行说明和上传 APK。
  - 删除重复的临时 Artifact 上传，README 改为 Releases 下载流程。
  - Ruby YAML 解析与 5 个 shell 区块语法检查通过，`git diff --check` 通过。
  - Godot `--check-only` 通过，完整核心回归输出 `PASS: core slice tests`。

### 阶段 46：Android 4.7 导出约束修复
- **状态：** complete
- 执行的操作：
  - 真实 GitHub Action 已推进到 Android 导出配置校验，证明前一轮 Java SDK 与 Android SDK 路径修复生效。
  - 用户日志稳定命中三项配置错误：标准导出模式覆盖 Min SDK、覆盖 Target SDK、缺少 ETC2/ASTC。
  - 新增针对这三个错误条件的快速配置回归，先在旧配置上确认红灯后再修复。
  - 红灯命令同时输出 `SDK overrides require Gradle build mode` 与 `ETC2/ASTC is required` 并以状态 1 退出。
  - `export_presets.cfg` 清空 Min/Target SDK 覆盖，保持标准导出；`project.godot` 启用 ETC2/ASTC。
  - 修复后快速导出约束、workflow YAML、5 个 shell 区块、Godot `--check-only` 与完整核心回归全部通过。
  - Android 预设 PCK 导出成功且文件非空；本机无 Android SDK，因此完整 APK 仍由 GitHub Action 复验。

### 阶段 47：地图掩体与出生安全
- **状态：** complete
- 执行的操作：
  - 真实感知探针覆盖 20 个种子：14/20 在出生瞬间已被一名警卫看到，出生附近一枪可惊动最多 3 名，侧翼出口一枪可惊动最多 7 名。
  - 根因是地图仅有少量墙线，且旧测试只验证玩家看不到警卫，没有验证警卫看不到玩家。
  - 新增出生零暴露、首段路线最多双敌暴露和至少 24 个导航障碍的回归边界。
  - 新增三面不可破坏砖墙形成出生安全庭院，保留左右两个离场出口。
  - 新增 18 段可破坏木墙，将左右侧翼、中场和北侧目标方向切成错位交战区；全图共有 23 段可破坏木墙。
  - 新地图使出生零暴露、首段最多双敌暴露和障碍密度红灯全部转绿。
  - 完整核心回归输出 `PASS: core slice tests`，证明导航、掩体、环境连锁、随机布局和任务闭环保持有效。
  - 沙箱内 Git 无法创建 `.git/index.lock`；改用已授权的非沙箱 Git 路径提交。

### 阶段 48：即时射击反馈
- **状态：** complete
- 执行的操作：
  - 诊断确认 `weapon.fired` 没有任何消费者，项目也没有枪声音频、枪口火光、曳光、后坐或命中标记。
  - 新增共享 `GameShotFeedback3D`，只在武器真实成功发射后播放反馈，换枪时重新绑定当前武器。
  - 枪口火光持续 70 ms、射线曳光 60 ms、命中闪光 100 ms；枪身产生短后坐并快速恢复。
  - 使用运行时生成的短促枪声，不依赖外部音频素材；Android 成功开火时轻震 30 ms。
  - headless 环境跳过纯音频播放器创建，避免测试退出对象泄漏；完整回归输出 `PASS: core slice tests`。

### 阶段 49：移动响应调优
- **状态：** complete
- 执行的操作：
  - 玩家最高速度由 4.5 m/s 提高到 6.0 m/s，明显拉开与 4.0 m/s 警卫的机动差距。
  - 移动摇杆半径由 72 px 缩小到 58 px；加入 12% 死区重映射和平方根曲线，使半程拖动达到约 66% 输出。
  - 自动验证半程输出不低于 62%、满程输出 100%、8% 漂移保持为零，以及玩家速度为 6.0 m/s。
  - Godot 语法检查与完整核心回归通过，输出 `PASS: core slice tests`。

### 阶段 50：双向受击反馈
- **状态：** complete
- 执行的操作：
  - 武器新增 `damage_confirmed`，射线只有在目标实际扣血后才发送，普通墙面碰撞不会伪造敌人命中确认。
  - 玩家与警卫场景接入共享 `GameDamageFeedback3D`，监听各自真实 `health.damaged`。
  - 警卫受击时模型闪红；玩家受击时模型闪红并叠加屏幕红色闪层、短促受击音和 Android 55 ms 震动。
  - 玩家实际造成伤害时显示 0.3 米、140 ms 的高亮命中标记，明显区别于普通撞击点。
  - 自动验证玩家和警卫的受伤信号均立即触发各自反馈组件；Godot 语法检查与完整核心回归通过。

### 阶段 51：原型兵人模型接入
- **状态：** complete
- 执行的操作：
  - 用户将共享兵人原型放入 `assets/models/prototypes/player.glb`，要求创建玩家/敌人预制体并替换现有 PrimitiveMesh。
  - 先通过 Godot 实际导入场景检查模型层级、网格表面和本地包围盒，再确定缩放与染色实现。
  - GLB 导入为 `player/world/geometry_0` 单网格，尺寸约 0.65×1.0×0.81 米且只有一个材质表面。
  - 创建共享兵人表现预制体，并由玩家/敌人预制体分别配置青绿色与红色材质实例。
  - 玩家和警卫场景移除胶囊体、方块枪预览网格，换成对应 GLB 预制体；碰撞体和逻辑枪口保持不变。
  - 使用整网格运行时材质绕开 dummy renderer 的空导入材质错误；语法检查和完整核心回归均通过。

### 阶段 52：环境原型模型接入
- **状态：** complete
- 执行的操作：
  - 用户已将 BrickWall、WoodenWall、WoodenCrate、Sandbag、OilDrum、FuelCan、Medkit、MissionTerminal 和 ExtractionBeacon 模型放入原型目录。
  - 计划先检查实际 GLB 层级与尺度，再创建表现预制体并替换地图视觉，玩法碰撞和领域逻辑保持不变。
  - 9 个 GLB 均确认是带 StandardMaterial3D 的单网格；已按实际 AABB 创建独立表现预制体，统一修正居中原点、地面高度和世界尺度。
  - 木墙、油桶墙、汽油桶墙、急救包、任务终端和撤离信标已替换旧 PrimitiveMesh；砖墙改为重复 2 米标准模型，避免拉伸砖块。
  - 新增木箱与沙袋静态掩体预制体并放入地图，纳入碰撞、导航障碍和盲区静态表现。
  - 静态盲区材质逻辑增加导入 GLB 表面材质回退，保证没有 `material_override` 的模型仍可暗化。
  - 自动测试补齐砖墙、木墙、木箱、沙袋、两类桶、急救包、任务终端和撤离信标的真实模型节点断言，并确认旧预览节点已移除。
  - 沙箱外 Godot 语法检查与完整核心回归通过，输出 `PASS: core slice tests`。

### 阶段 53：Blender 模型贴图明度审计与修复
- **状态：** in_progress
- 执行的操作：
  - 用户要求使用 Blender 检查并处理 `assets/models` 下因贴图暗部造成环境中细节不足的模型。
  - 确认目录包含 10 个 GLB、10 张对应提取贴图，Blender 版本为 4.5.12 LTS。
  - 当前工作区还包含用户在 Godot 编辑器保存的场景序列化变化和上一轮未提交的视野内纹理补光；资产处理必须保留这些改动。
  - 新增只读 Blender 审计脚本，批量提取 GLB 网格规模、材质节点、贴图色彩空间、亮度分位数与暗像素比例。
  - 首轮审计成功：10 个 GLB 均为单网格/单材质/1024² sRGB 嵌入贴图，但全部错误使用 Metallic=1、Roughness=1；决定先做材质单变量修复，暂不改贴图像素。
  - 新增 Blender 材质修复脚本，可将 GLB 中所有 Principled BSDF 的 Metallic 设为 0，并输出到独立目录供结构复检后再覆盖源资产。
  - 临时常规重导出保持三角形数但拆分共享顶点，GLB 体积增加 47%–119%；已拒绝该输出并将脚本改为只修改 glTF JSON 材质字段，BIN chunk 保持逐字节不变。
  - 外科式临时输出复检通过：10 个模型全部为 Metallic=0，网格/三角形/贴图统计与原始值一致，文件仅增加 16–20 字节；已将同一修补应用到源 GLB。
  - Godot 成功重新导入全部模型；关闭运行时自发光后，固定木墙探针亮度 0.3663、对比度 0.1289 并通过，材质颜色和表面细节均明显改善。
  - 删除视野内自发光补丁，保留近白环境光与 1.6 能量；新增 `MODEL_AUDIT.md` 记录逐模型审计和处理结论。
  - 最终 Blender 前后对比 `all_pass=true`；Godot 完整核心回归输出 `PASS: core slice tests`，最终视觉探针稳定为亮度 0.3663 / 对比度 0.1289。
  - 阶段 53 完成；未提交，`.codex/rules/bash.rules` 与主场景其余机械序列化变化保持用户当前状态。

### 阶段 54：弹着点与可破坏物生命条
- **状态：** complete
- 执行的操作：
  - 用户要求砖墙命中也显示弹着点，并为可破坏物增加相应生命条。
  - 确认射线已经命中砖墙，但普通撞击球正好位于碰撞面，存在被墙面遮住的问题。
  - 确认木墙、石油桶墙和汽油桶墙均复用 `HealthComponent`，可由共享组件统一监听并显示剩余耐久。
  - 新增 `GameWorldHealthBar3D` 并接入三类场景；首次完整测试因 Godot 尚未刷新新 `class_name` 缓存而解析失败，下一步先执行 headless 编辑器导入后复验。
  - 普通弹着点持续时间提高到 300 ms，并沿碰撞点朝射手偏移 4.5 cm，避免砖墙表面深度遮挡。
  - 生命条默认隐藏，真实受伤后显示 1.8 秒，按剩余耐久显示绿/黄/红并面向相机；盲区材质服务明确跳过生命条网格。
  - 自动验证三类场景均接入生命条、木墙/石油桶/汽油桶比例正确、超时隐藏以及弹着点表面偏移。
  - Godot headless 编辑器刷新全局类后，`--check-only` 与完整核心回归通过；Forward Mobile 视觉探针确认生命条位置、比例和颜色正确。

### 阶段 55：射线选敌与持续锁定交互
- **状态：** complete
- 执行的操作：
  - 用户确认将移动端交互改为射线瞄准吸附并持续锁定敌人，避免右手反复控制瞄准与射击。
  - 确认当前实现是右摇杆与可拖动射击键并存，瞄准辅助仅做逐帧轻度修正且没有锁定状态。
  - 确认本轮必须保持固定镜头、120° 受限视野、墙体遮挡与 10 米武器射程；锁定不得发现盲区或墙后目标，火箭仍为直线弹道。
  - 新增 ADR-078 并同步修订 ADR-006、ADR-007 与 GDD；首次代码补丁因同一文件同时 Delete/Add 被工具拒绝，未产生代码变更，已改用整段更新方案。
  - 首轮回归发现一处旧测试仍以无参数调用 `_update_aim_direction`，并且新的遮挡测试清晰正例实际落在 z=-3 木墙后；其余盲区失败是该运行时错误留下场景后的级联污染，下一轮先修测试隔离与位置。
  - 独立射线探针确认锁定方向正确；首次锁定射击未扣血是测试移动警卫后未推进物理帧，宽相位仍保留旧位置，并非玩法代码故障。
  - 修正探针位置与物理同步后仅剩遮挡宽限的指示器可见性失败；根因是有效性检查隐藏后，方向解析末尾又无条件显示，已改为按遮挡累计时间决定指示器是否可见。
  - 完整核心回归已通过；Forward Mobile 视觉探针确认锁定射线正确变黄并持续指向目标，随后放大菱形锁定标记以提高移动端可读性。
  - 移动端帮助文字同步为“左摇杆移动、右摇杆选敌并锁定、射击键开火”；README、GDD 与 ADR 已统一新交互语义。
  - 最终 `git diff --check`、Godot `--check-only` 和完整核心回归通过，输出 `PASS: core slice tests`；放大后的锁定标记视觉复验清晰可读。
  - 最终串行复跑首次命中已知 macOS 沙箱 `user://logs` / MoltenVK signal 11（代码验证此前已通过）；按既有处理改用授权的非沙箱 headless 命令复验，不在相同沙箱路径重复。
  - 非沙箱 `--check-only` 与完整核心回归复验通过，最终输出 `PASS: core slice tests`。

### 阶段 56：缩短视野扇区半径
- **状态：** complete
- 执行的操作：
  - 用户反馈角色朝屏幕下方时，视野扇区受近景透视影响覆盖屏幕过大，要求再次缩小视野范围。
  - 将玩家与警卫共享的 120° 视野半径由 12 米同步缩短为 11 米，扇区绘制、感知、锁定候选和动态物品显隐继续复用同一传感器参数。
  - 保持所有武器 10 米射程不变，视野仍严格大于射程并保留 1 米观察缓冲。
  - 更新 GDD、术语表、现有视野/射程 ADR、README 与自动回归断言。
  - `git diff --check`、Godot `--check-only` 与完整核心回归均通过，输出 `PASS: core slice tests`。

### 阶段 57：玩家移动速度回调
- **状态：** complete
- 执行的操作：
  - 用户按约 1 米 Player 尺度确认当前 6.0 米/秒偏快，并接受回调至 5.0 米/秒的建议。
  - Player 基础移动速度改为 5.0 米/秒；警卫战斗移动仍为 4.0 米/秒，玩家保留 25% 机动优势。
  - 2 米地图模块的满速穿越时间由约 0.33 秒增加到 0.4 秒，10 米攻击范围的穿越时间由约 1.67 秒增加到 2 秒。
  - 同步 GDD、ADR-068、README、术语表、规划记录与自动回归断言。
  - `git diff --check`、Godot `--check-only` 与完整核心回归均通过，输出 `PASS: core slice tests`。

### 阶段 58：非对称 CQB 视距与射程
- **状态：** complete
- 执行的操作：
  - 用户指定 Player 改为 10 米视野、8 米射程，警卫改为 8 米视野、8 米射程。
  - Player 与警卫继续共享 120° 夹角和墙体遮挡算法，但场景分别覆盖 10 米与 8 米视野半径。
  - 标准手枪、重型手枪、机枪和火箭统一调整为 8 米射程；常驻瞄准线、锁定候选、即时射线与火箭最大航程自动服从武器定义。
  - 新增 ADR-079，明确部分取代 ADR-043 的等距视野和 ADR-077 的双方视野严格大于射程规则。
  - 自动断言新增 9 米处 Player 仍能看见警卫、警卫无法看见 Player 的非对称边界。
  - 首轮完整回归发现旧遮挡宽限探针将目标放在距 Player 9 米处，新规则会先按 8 米射程正确解除锁定；将 Player 移近墙体，使墙后目标保持在 7 米内后，遮挡宽限语义恢复准确。
  - `git diff --check`、Godot `--check-only` 与完整核心回归均通过，最终输出 `PASS: core slice tests`。

### 阶段 59：房间优先的 CQB 地图重构
- **状态：** in_progress
- 执行的操作：
  - 用户确认当前地图对 CQB 仍偏空旷，要求按“小房间划分后再添加室内障碍物”的方向重构。
  - 使用 planning-with-files-zh 恢复并完整读取现有规划文件；session catchup 未报告遗漏上下文。
  - 冻结本轮边界：保留 2 米导航网格、任务/撤离流程、随机内容预算、可破坏环境和非对称视距，只重构主场景空间拓扑及候选点。
  - 完整审读主场景前 940 行：地图为 60×40 米；现有不可破坏结构主要是边界、出生庭院、中央砖墙和少量外围 L 形墙，23 段木墙更多承担屏风而非房间边界。
  - 确认空旷感根因是宏观房间闭合度不足，而不是单纯缺少掩体；固定木箱/沙袋约 10 个且多位于外围。
  - 确认随机候选点需与空间拓扑同步重排，避免警卫、任务和物资继续集中在外围开放区域。
  - 审计可复用结构：砖墙线支持任意偶数米长度、横竖方向和导航阻塞；木箱为 1.2 米方形占地，沙袋为 2×0.65 米占地，均可直接用于室内微观掩体。
  - 审计生成与导航：内容生成器只消费 Marker3D 变换；网格覆盖完整 60×40 米、2 米正交移动，适合直接重排候选点而无需改生成算法。
  - 现有地图回归尚未覆盖房间数量、门洞宽度、最长直视线和全部候选点可达性，本轮需补这些验收边界。
  - 新建独立 `cqb_room_layout_3d.tscn`：29 段不可破坏砖墙线形成三条横向门槛与四条纵向分隔带，门洞错位；14 个木箱/沙袋作为室内微观掩体。
  - 布置 12 个房间锚点和 10 个门洞锚点，供拓扑回归直接验证；新场景通过 Godot `--check-only`。
  - 主场景已实例化独立 CQB 房间布局，并移除旧中央长砖墙、外围 L 形建筑和散落固定掩体，避免新旧拓扑重叠。
  - 23 段可破坏木墙全部保留，其中侧翼与北部屏风重新移入房间内部；石油→木墙→汽油连锁移入西南房间且继续保持 2 米严格邻接。
  - 修复主场景仍需使用出生庭院砖墙视觉资源的依赖后，Godot `--check-only` 通过。
  - 将 16 个警卫、10 个武器和 8 个急救包候选点重排到北/中/南房间；四个任务候选继续位于最远端，保留至少 36 米直线距离要求。
  - 地图测试改为验证独立布局、29 段房间墙、14 个室内掩体、12 个房间锚点、10 个门洞锚点、门洞开放、候选点可达及房间两轴邻近遮挡。
  - Godot 语法检查通过；首轮完整回归失败集中在地图连通性：绝大多数候选点从出生格无路径，3 个南部门洞锚点被判为阻塞，证明物理门洞宽度没有正确映射到 2 米导航格。
  - 同轮还发现一条旧导航断言仍依赖已删除的中央长砖墙坐标，需要改为检查新房间墙占格。
  - 新增 `cqb_navigation_probe.gd` 输出完整 30×20 阻塞/连通 ASCII 图与全部候选点状态；确认只有中央分量可达，且多个偶数坐标候选与偶数坐标掩体落入同一扩张占格。
  - 修复策略改为网格对齐重排：墙视觉模块与内容落在奇数格中心，门洞保留两个连续开放格，不逐个为候选点打补丁。
  - 整体重排后 Godot 语法检查通过；第二次导航探针显示主连通分量已覆盖地图绝大部分，全部任务、武器、急救包和 14/16 警卫候选可达。
  - 剩余两个不可达警卫位于左右中间房，根因收窄为旧 `WestScreen/EastScreen` 仍位于偶数坐标并额外覆盖两列导航格；下一步将其对齐奇数格而非移动候选逃避问题。
  - 将左右中间房屏风与北部掩体统一对齐奇数格中心后，第三次导航探针确认 16/16 警卫、4/4 任务、10/10 武器和 8/8 急救包候选全部从出生点可达；房间拓扑连通性修复完成。
  - 首次连通性修复后的完整回归仅剩 `DoorSouthInnerEast` 锚点阻塞；确认南部内侧门锚点误放在门槛后方 z=2，而实际门槛通行格中心为 z=1，已将东西两个对称锚点统一校正到门槛平面。
  - 门锚点修正后完整核心回归通过，输出 `PASS: core slice tests`；新增独立高位正交相机视觉探针，准备用无 HUD、无动态单位截图验收房间密度与可读性。
  - 视觉探针在 headless 默认与 `gl_compatibility` 两种调用下都进入 dummy 渲染器，viewport 纹理为空；记录为环境限制，改用非 headless 离屏窗口执行同一探针，不修改地图规避。
  - 非 headless Compatibility 渲染成功生成 1280×720 俯视图 `/tmp/gogogo-cqb-room-layout.png`；视觉复核确认三条横向门槛与四条错位纵向分隔带形成清晰房间链，中央出生庭院紧凑、两侧循环可读、室内掩体密度克制且门洞未被视觉遮死。
  - 同步 GDD、README 与术语表，并新增 ADR-080，冻结房间优先、错位双格门洞、左右循环路线和全部随机候选可达等地图规则；核心交互与数值边界保持不变。
  - 最终 `git diff --check`、Godot `--check-only`、完整导航探针和核心回归全部通过；38 个随机内容候选均可达，核心回归输出 `PASS: core slice tests`，阶段 59 收口。

## 本轮 Android CI 错误日志
| 时间戳 | 错误 | 尝试次数 | 解决方案 |
|--------|------|---------|---------|
| 2026-08-26 | 环境 GLB 材质探针在沙箱内无法打开 `user://logs` 并触发 MoltenVK signal 11 | 1 | 不在沙箱内重复；改用已授权的沙箱外 Godot headless 路径完成只读检查 |
| 2026-08-26 | Docker CLI 无法连接 OrbStack daemon，本机无法启动 `barichello/godot-ci:4.7` 重放 | 1 | 不重复尝试；使用用户的远端日志为原始反馈，新增快速环境自检作为本地回归缝隙 |
| 2026-08-26 | 沙箱网络无法解析 `raw.githubusercontent.com` | 1 | 按工具规则改为申请非沙箱网络读取镜像源配置 |
| 2026-08-26 | 本机旧版 Ruby 缺少 `Array#filter_map`，shell 区块批量语法检查未执行 | 1 | YAML 解析已通过；改用 `map { ... }.compact` 后重新检查，不重复使用不兼容 API |
| 2026-08-26 | 首次合并规划补丁因 findings.md 原文上下文不匹配而未应用 | 1 | 重新读取精确上下文，拆分并重放更小补丁 |
| 2026-08-26 | 本地 PCK 导出结束时无法写用户级 EditorSettings，并报告 macOS 系统 CA 读取错误 | 1 | PCK 已成功生成且非空；两项均为受限本机环境噪声，不影响配置回归或远端 Linux APK 导出 |
| 2026-08-26 | 最终完整回归复跑无法打开 `user://logs` 并在 MoltenVK 路径 signal 11 | 2 | 兼容渲染后端仍失败，确认是沙箱用户目录限制；沙箱外复跑输出 `PASS: core slice tests` |
